@using System.Diagnostics
@{
    var currentController = ViewContext.RouteData.Values[key: "controller"] as string;
    var currentAction = ViewContext.RouteData.Values[key: "action"] as string;

    Debug.Assert(condition: User.Identity != null, message: "User.Identity != null");
}


<div class="dropdown">
    @* <span class="material-symbols-outlined">settings</span> *@
    <a class="nav-link dropdown-toggle @(currentController is "TeacherCourses" or "Teachers" ? "active" : "")"
       role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="material-symbols-outlined">admin_panel_settings</i>
        Teachers
    </a>

    <ul class="dropdown-menu">
        <li>
            <a class="dropdown-item @(currentController == "Teachers" ? "active" : "")"
               asp-area="" asp-controller="Teachers" asp-action="Index">
                <i class="fas fa-person-through-window"></i> Teachers
            </a>
        </li>
        <li>
            <hr class="dropdown-divider">
        </li>
        <li>
            <a class="dropdown-item @(currentController == "TeacherCourses" ? "active" : "")"
               asp-area="" asp-controller="TeacherCourses" asp-action="Index">
                <i class="fas fa-person-through-window"></i> TeacherCourses
            </a>
        </li>
    </ul>

</div>